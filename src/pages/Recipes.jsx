import React from 'react';
import Header from '../components/header/Header';

const Recipes = () => {
    return (
        <div>
            <Header/>
        </div>
    );
};

export default Recipes;